import React, { useCallback, useMemo, useState } from 'react';
import { ActivityIndicator, Modal, Platform, ScrollView, StyleSheet, Text, TextInput, TouchableOpacity, View } from 'react-native';
import { useFocusEffect } from '@react-navigation/native';
import API from '../api/api';
import { CenterInventoryItem, CenterWarehouse, WarehouseObject, WarehouseObjectType } from '../types';

const CELL = 28;
const OBJECT_TYPES: Array<{ type: WarehouseObjectType; label: string; color: string }> = [
  { type: 'rack', label: 'Rack', color: '#123b52' },
  { type: 'shelf', label: 'Shelf', color: '#0b777b' },
  { type: 'workbench', label: 'Workbench', color: '#7b5a1d' },
  { type: 'zone', label: 'Free zone', color: '#5c6f7b' },
  { type: 'compressor', label: 'Compressor', color: '#7a3d47' },
];
const titleFor = (type: WarehouseObjectType) => OBJECT_TYPES.find((item) => item.type === type)?.label || 'Object';
const ROTATABLE_TYPES: WarehouseObjectType[] = ['rack', 'shelf', 'workbench'];
const BUGGY_DEFAULT_SIZES: Partial<Record<WarehouseObjectType, { width: number; height: number }>> = {
  rack: { width: 10, height: 6 }, shelf: { width: 8, height: 5 }, workbench: { width: 9, height: 5 }, zone: { width: 12, height: 8 }, compressor: { width: 6, height: 6 },
};
const rotatedFootprint = (object: WarehouseObject) => {
  const angle = ((object.rotation || 0) % 180 + 180) % 180;
  if (angle === 90) return { width: object.height, height: object.width };
  if (angle === 45 || angle === 135) {
    const diagonal = Math.ceil((object.width + object.height) / Math.sqrt(2));
    return { width: diagonal, height: diagonal };
  }
  return { width: object.width, height: object.height };
};

export default function CenterWarehouseScreen() {
  const [data, setData] = useState<CenterWarehouse | null>(null);
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [modal, setModal] = useState(false);
  const [mapSettings, setMapSettings] = useState(false);
  const [mapName, setMapName] = useState('');
  const [mapWidth, setMapWidth] = useState('24');
  const [mapHeight, setMapHeight] = useState('16');
  const [query, setQuery] = useState('');
  const [showUnassignedOnly, setShowUnassignedOnly] = useState(false);
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState('');
  const load = useCallback(async () => { setLoading(true); setError(''); try { const response = await API.get<CenterWarehouse>('/center/warehouse'); setData(response.data); setMapName(response.data.map.name); setMapWidth(String(response.data.map.width)); setMapHeight(String(response.data.map.height)); } catch (err: any) { setError(err?.response?.data?.message || 'Could not load the warehouse map.'); } finally { setLoading(false); } }, []);
  useFocusEffect(useCallback(() => { load(); }, [load]));
  const selected = data?.objects.find((object) => object.id === selectedId) || null;
  const selectedInventory = useMemo(() => data?.inventory.filter((item) => item.warehouseObjectId === selectedId) || [], [data, selectedId]);
  const filteredInventory = useMemo(() => { const normalized = query.trim().toLowerCase(); return data?.inventory.filter((item) => (!showUnassignedOnly || item.warehouseObjectId !== null) && (!normalized || `${item.name} ${item.category}`.toLowerCase().includes(normalized))) || []; }, [data, query, showUnassignedOnly]);
  const addObject = async (type: WarehouseObjectType) => { const definition = OBJECT_TYPES.find((item) => item.type === type)!; setSaving(true); try { const response = await API.post<WarehouseObject>('/center/warehouse/objects', { type, label: `${definition.label} ${(data?.objects.filter((item) => item.type === type).length || 0) + 1}`, color: definition.color, ...BUGGY_DEFAULT_SIZES[type] }); setData((current) => current ? { ...current, objects: [...current.objects, response.data] } : current); setSelectedId(response.data.id); } catch (err: any) { setError(err?.response?.data?.message || 'Could not add this warehouse object.'); } finally { setSaving(false); } };
  const patchObject = async (changes: Partial<WarehouseObject>) => { if (!selected) return; try { const response = await API.patch<WarehouseObject>(`/center/warehouse/objects/${selected.id}`, changes); setData((current) => current ? { ...current, objects: current.objects.map((object) => object.id === selected.id ? response.data : object) } : current); } catch (err: any) { setError(err?.response?.data?.message || 'Could not update the selected object.'); } };
  const move = (dx: number, dy: number) => { if (!selected || !data) return; const footprint = rotatedFootprint(selected); patchObject({ x: Math.max(0, Math.min(data.map.width - footprint.width, selected.x + dx)), y: Math.max(0, Math.min(data.map.height - footprint.height, selected.y + dy)) }); };
  const remove = async () => { if (!selected) return; try { await API.delete(`/center/warehouse/objects/${selected.id}`); setData((current) => current ? { ...current, objects: current.objects.filter((object) => object.id !== selected.id), inventory: current.inventory.map((item) => item.warehouseObjectId === selected.id ? { ...item, warehouseObjectId: null } : item) } : current); setSelectedId(null); } catch { setError('Could not remove this warehouse object.'); } };
  const rotate = () => { if (!selected || !ROTATABLE_TYPES.includes(selected.type)) return; patchObject({ rotation: ((selected.rotation || 0) + 45) % 360 }); };
  const clearAll = async () => { if (Platform.OS === 'web' && !window.confirm('Delete every object from this warehouse map? Inventory items will remain unassigned.')) return; try { await API.delete('/center/warehouse/objects'); setData((current) => current ? { ...current, objects: [], inventory: current.inventory.map((item) => ({ ...item, warehouseObjectId: null })) } : current); setSelectedId(null); } catch (err: any) { setError(err?.response?.data?.message || 'Could not clear the warehouse map.'); } };
  const assign = async (item: CenterInventoryItem) => { if (!selected || !data) return; const warehouseObjectId = item.warehouseObjectId === selected.id ? null : selected.id; try { await API.patch(`/center/inventory/${item.id}`, { warehouseObjectId }); setData((current) => current ? { ...current, inventory: current.inventory.map((candidate) => candidate.id === item.id ? { ...candidate, warehouseObjectId } : candidate) } : current); } catch { setError('Could not update this inventory location.'); } };
  const saveMapSettings = async () => { const width = Number(mapWidth); const height = Number(mapHeight); if (!Number.isInteger(width) || !Number.isInteger(height) || width < 6 || height < 6) { setError('Warehouse dimensions must be whole numbers of at least 6 squares.'); return; } try { const response = await API.patch('/center/warehouse', { name: mapName.trim() || 'Main warehouse', width, height }); setData((current) => current ? { ...current, map: response.data } : current); setMapSettings(false); } catch (err: any) { setError(err?.response?.data?.message || 'Could not save the warehouse dimensions.'); } };
  if (loading && !data) return <View style={styles.center}><ActivityIndicator color="#123b52" /><Text style={styles.loading}>Loading warehouse map…</Text></View>;
  if (!data) return <View style={styles.center}><Text accessibilityRole="alert" style={styles.error}>{error}</Text><TouchableOpacity style={styles.retry} onPress={load}><Text style={styles.retryText}>Retry</Text></TouchableOpacity></View>;
  return <View style={styles.screen}><ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled">
    <View style={styles.hero}><View style={styles.heroCopy}><Text style={styles.eyebrow}>CENTER OPERATIONS</Text><Text style={styles.title}>Warehouse layout</Text><Text style={styles.subtitle}>Design your storage floor, then open a rack to see the inventory assigned to it.</Text></View><View style={styles.mapBadge}><Text style={styles.mapBadgeValue}>{data.objects.length}</Text><Text style={styles.mapBadgeLabel}>map objects</Text></View></View>
    {error ? <Text accessibilityRole="alert" style={styles.error}>{error}</Text> : null}
    <View style={{ flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', minWidth: 620, marginTop: 12 }}><Text style={{ color: '#718394', fontSize: 11 }}>Layout tools</Text><TouchableOpacity accessibilityRole="button" style={{ borderWidth: 1, borderColor: '#b42318', borderRadius: 16, paddingHorizontal: 12, paddingVertical: 8 }} onPress={clearAll}><Text style={{ color: '#b42318', fontSize: 12, fontWeight: 'bold' }}>Delete all objects</Text></TouchableOpacity></View>
    <View style={styles.toolbar}><Text style={styles.toolbarTitle}>Add to the map</Text><View style={[styles.toolRow, { flexWrap: 'nowrap' }]}>{OBJECT_TYPES.map((item) => <TouchableOpacity key={item.type} accessibilityRole="button" style={[styles.toolButton, { borderColor: item.color }]} disabled={saving} onPress={() => addObject(item.type)}><Text style={[styles.toolText, { color: item.color }]}>+ {item.label}</Text></TouchableOpacity>)}</View></View>
    <View style={styles.mapPanel}><View style={[styles.mapHeader, { minWidth: 620 }]}><View><Text style={styles.sectionTitle}>{data.map.name}</Text><Text style={styles.sectionHint}>{data.map.width} × {data.map.height} squares · Select an object to edit it.</Text></View><View style={styles.mapHeaderActions}><Text style={styles.legend}>BLUEPRINT</Text><TouchableOpacity accessibilityRole="button" style={styles.settingsButton} onPress={() => setMapSettings(true)}><Text style={styles.settingsText}>Edit map</Text></TouchableOpacity></View></View><ScrollView style={styles.mapViewport} nestedScrollEnabled showsVerticalScrollIndicator><ScrollView horizontal scrollEnabled={false} showsHorizontalScrollIndicator contentContainerStyle={styles.mapScroll}><View style={[styles.map, { width: data.map.width * CELL, height: data.map.height * CELL, overflow: 'hidden' }]}>{Array.from({ length: data.map.width * data.map.height }, (_, index) => <View key={index} style={styles.gridCell} />)}{data.objects.map((object) => { const objectSize = object.type === 'compressor' ? Math.max(object.width, object.height) : object.width; const objectHeight = object.type === 'compressor' ? objectSize : object.height; return <TouchableOpacity key={object.id} accessibilityRole="button" accessibilityState={{ selected: selectedId === object.id }} onPress={() => setSelectedId(object.id)} style={[styles.mapObject, { left: object.x * CELL, top: object.y * CELL, width: objectSize * CELL - 4, height: objectHeight * CELL - 4, backgroundColor: object.color, transform: [{ rotate: `${object.rotation || 0}deg` }] }, selectedId === object.id && styles.mapObjectSelected]}><Text numberOfLines={2} style={styles.mapObjectLabel}>{object.label}</Text><Text style={styles.mapObjectCount}>{data.inventory.filter((item) => item.warehouseObjectId === object.id).length} assets</Text></TouchableOpacity>; })}</View></ScrollView></ScrollView></View>
    {selected ? <View style={styles.editor}><View style={styles.editorHeader}><View style={styles.editorCopy}><Text style={styles.sectionTitle}>{selected.label}</Text><Text style={styles.sectionHint}>{titleFor(selected.type)} · {selected.width} × {selected.height} squares · {selected.rotation || 0}°</Text></View><TouchableOpacity accessibilityRole="button" onPress={remove}><Text style={styles.delete}>Remove</Text></TouchableOpacity></View><Text style={styles.label}>Label</Text><TextInput accessibilityLabel="Warehouse object label" value={selected.label} onChangeText={(value) => patchObject({ label: value })} style={styles.input} /><View style={styles.controls}><View><Text style={styles.controlLabel}>Position</Text><View style={styles.arrowGrid}><TouchableOpacity onPress={() => move(0, -1)} style={styles.arrow}><Text style={styles.arrowText}>↑</Text></TouchableOpacity><TouchableOpacity onPress={() => move(-1, 0)} style={styles.arrow}><Text style={styles.arrowText}>←</Text></TouchableOpacity><TouchableOpacity onPress={() => move(1, 0)} style={styles.arrow}><Text style={styles.arrowText}>→</Text></TouchableOpacity><TouchableOpacity onPress={() => move(0, 1)} style={styles.arrow}><Text style={styles.arrowText}>↓</Text></TouchableOpacity></View></View><View><Text style={styles.controlLabel}>Size & orientation</Text><View style={styles.sizeButtons}><TouchableOpacity style={styles.sizeButton} onPress={() => patchObject({ width: Math.max(1, selected.width - 1) })}><Text style={styles.sizeText}>− width</Text></TouchableOpacity><TouchableOpacity style={styles.sizeButton} onPress={() => patchObject({ width: Math.min(data.map.width, selected.width + 1) })}><Text style={styles.sizeText}>+ width</Text></TouchableOpacity><TouchableOpacity disabled={!ROTATABLE_TYPES.includes(selected.type)} style={[styles.sizeButton, !ROTATABLE_TYPES.includes(selected.type) && { opacity: 0.45 }]} onPress={rotate}><Text style={styles.sizeText}>Rotate 45°</Text></TouchableOpacity></View></View></View><TouchableOpacity accessibilityRole="button" style={styles.assignButton} onPress={() => setModal(true)}><Text style={styles.assignButtonText}>Manage assigned inventory ({selectedInventory.length})</Text></TouchableOpacity></View> : <View style={styles.empty}><Text style={styles.emptyTitle}>Select a rack or zone</Text><Text style={styles.emptyText}>Click any object on the map to position it or assign gear.</Text></View>}
    <TouchableOpacity accessibilityRole="switch" accessibilityState={{ checked: showUnassignedOnly }} onPress={() => setShowUnassignedOnly((value) => !value)} style={{ alignSelf: 'flex-start', borderWidth: 1, borderColor: '#aabfc7', borderRadius: 14, paddingHorizontal: 10, paddingVertical: 7, marginTop: 12 }}><Text style={{ color: '#0b777b', fontSize: 11, fontWeight: 'bold' }}>{showUnassignedOnly ? 'Showing unassigned only' : 'Filter unassigned inventory'}</Text></TouchableOpacity>
  </ScrollView>
  <Modal visible={modal} transparent animationType="slide" onRequestClose={() => setModal(false)}><View style={styles.backdrop}><View style={styles.modal}><View style={styles.modalHeader}><View><Text style={styles.modalTitle}>Assign inventory</Text><Text style={styles.modalHint}>{selected?.label || 'Selected object'}</Text></View><TouchableOpacity onPress={() => setModal(false)}><Text style={styles.close}>×</Text></TouchableOpacity></View><TextInput accessibilityLabel="Search warehouse inventory" placeholder="Search equipment" value={query} onChangeText={setQuery} style={styles.input} /><ScrollView style={styles.inventoryList}>{filteredInventory.map((item) => <TouchableOpacity key={item.id} accessibilityRole="checkbox" accessibilityState={{ checked: item.warehouseObjectId === selectedId }} onPress={() => assign(item)} style={[styles.inventoryRow, item.warehouseObjectId === selectedId && styles.inventoryRowActive]}><View style={styles.inventoryCheck}><Text style={styles.inventoryCheckText}>{item.warehouseObjectId === selectedId ? '✓' : ''}</Text></View><View style={styles.inventoryCopy}><Text style={styles.inventoryName}>{item.name}</Text><Text style={styles.inventoryMeta}>{item.category} · {item.quantity} unit{item.quantity === 1 ? '' : 's'}{item.warehouseObjectId && item.warehouseObjectId !== selectedId ? ' · Assigned elsewhere' : ''}</Text></View></TouchableOpacity>)}</ScrollView><TouchableOpacity style={styles.doneButton} onPress={() => setModal(false)}><Text style={styles.doneText}>Done</Text></TouchableOpacity></View></View></Modal>
  <Modal visible={mapSettings} transparent animationType="slide" onRequestClose={() => setMapSettings(false)}><View style={styles.backdrop}><View style={styles.modal}><View style={styles.modalHeader}><View><Text style={styles.modalTitle}>Warehouse canvas</Text><Text style={styles.modalHint}>Resize the grid without cutting off placed objects.</Text></View><TouchableOpacity onPress={() => setMapSettings(false)}><Text style={styles.close}>×</Text></TouchableOpacity></View><Text style={styles.label}>Map name</Text><TextInput accessibilityLabel="Warehouse map name" value={mapName} onChangeText={setMapName} style={styles.input} /><View style={styles.dimensionRow}><View style={styles.dimensionField}><Text style={styles.label}>Width (squares)</Text><TextInput accessibilityLabel="Warehouse map width" keyboardType="number-pad" value={mapWidth} onChangeText={setMapWidth} style={styles.input} /></View><View style={styles.dimensionField}><Text style={styles.label}>Height (squares)</Text><TextInput accessibilityLabel="Warehouse map height" keyboardType="number-pad" value={mapHeight} onChangeText={setMapHeight} style={styles.input} /></View></View><Text style={styles.dimensionHint}>Larger maps can be explored using horizontal and vertical scrolling.</Text><TouchableOpacity style={styles.doneButton} onPress={saveMapSettings}><Text style={styles.doneText}>Save canvas</Text></TouchableOpacity></View></View></Modal>
  </View>;
}

const styles = StyleSheet.create({
  screen: { flex: 1, backgroundColor: '#f3f6f8' }, content: { width: '100%', maxWidth: 1200, alignSelf: 'center', padding: 20, paddingBottom: 60 }, center: { flex: 1, justifyContent: 'center', alignItems: 'center', padding: 24 }, loading: { color: '#536b7a', marginTop: 10 }, error: { color: '#b42318', marginTop: 12 }, retry: { borderWidth: 1, borderColor: '#123b52', borderRadius: 18, paddingHorizontal: 16, paddingVertical: 9, marginTop: 14 }, retryText: { color: '#123b52', fontWeight: 'bold' }, hero: { flexDirection: 'row', flexWrap: 'wrap', justifyContent: 'space-between', alignItems: 'center', gap: 16, backgroundColor: '#e1ebee', borderWidth: 1, borderColor: '#b9cdd3', borderRadius: 22, padding: 24 }, heroCopy: { flex: 1, minWidth: 260 }, eyebrow: { color: '#0b777b', fontSize: 11, fontWeight: 'bold', letterSpacing: 1.2 }, title: { color: '#123b52', fontSize: 29, fontWeight: 'bold', marginTop: 5 }, subtitle: { color: '#536b7a', lineHeight: 21, marginTop: 7 }, mapBadge: { width: 86, height: 86, borderRadius: 43, backgroundColor: '#fff', alignItems: 'center', justifyContent: 'center' }, mapBadgeValue: { color: '#123b52', fontSize: 22, fontWeight: 'bold' }, mapBadgeLabel: { color: '#718394', fontSize: 10, marginTop: 2 }, toolbar: { backgroundColor: '#fff', borderWidth: 1, borderColor: '#ccdbe0', borderRadius: 16, padding: 15, marginTop: 16 }, toolbarTitle: { color: '#123b52', fontWeight: 'bold' }, toolRow: { flexDirection: 'row', flexWrap: 'wrap', gap: 8, marginTop: 10 }, toolButton: { borderWidth: 1, borderRadius: 18, paddingHorizontal: 12, paddingVertical: 9 }, toolText: { fontSize: 12, fontWeight: 'bold' }, mapPanel: { backgroundColor: '#fff', borderWidth: 1, borderColor: '#ccdbe0', borderRadius: 17, padding: 16, marginTop: 16 }, mapHeader: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'flex-start', gap: 10 }, mapHeaderActions: { flexDirection: 'row', alignItems: 'center', gap: 10 }, settingsButton: { borderWidth: 1, borderColor: '#0b777b', borderRadius: 16, paddingHorizontal: 10, paddingVertical: 7 }, settingsText: { color: '#0b777b', fontSize: 11, fontWeight: 'bold' }, sectionTitle: { color: '#123b52', fontSize: 18, fontWeight: 'bold' }, sectionHint: { color: '#718394', fontSize: 12, marginTop: 4 }, legend: { color: '#0b777b', fontSize: 10, fontWeight: 'bold', letterSpacing: 1 }, mapViewport: { maxHeight: 520, marginTop: 12 }, mapScroll: { padding: 14 }, map: { flexDirection: 'row', flexWrap: 'wrap', position: 'relative', backgroundColor: '#f7fafb', borderWidth: 1, borderColor: '#aabfc7' }, gridCell: { width: CELL, height: CELL, borderRightWidth: 1, borderBottomWidth: 1, borderColor: '#dbe7ea' }, mapObject: { position: 'absolute', borderRadius: 7, padding: 5, justifyContent: 'center', alignItems: 'center', borderWidth: 2, borderColor: 'rgba(255,255,255,.8)' }, mapObjectSelected: { borderColor: '#f3bb4b', borderWidth: 3, shadowColor: '#123b52', shadowOpacity: 0.3, shadowRadius: 6, elevation: 4 }, mapObjectLabel: { color: '#fff', fontWeight: 'bold', fontSize: 11, textAlign: 'center' }, mapObjectCount: { color: '#e7f3f5', fontSize: 9, marginTop: 2 }, editor: { backgroundColor: '#fff', borderWidth: 1, borderColor: '#ccdbe0', borderRadius: 17, padding: 18, marginTop: 16 }, editorHeader: { flexDirection: 'row', justifyContent: 'space-between', gap: 12 }, editorCopy: { flex: 1 }, delete: { color: '#b42318', fontWeight: 'bold' }, label: { color: '#123b52', fontSize: 12, fontWeight: 'bold', marginTop: 14, marginBottom: 6 }, input: { minHeight: 45, borderWidth: 1, borderColor: '#aabfc7', borderRadius: 10, paddingHorizontal: 12, paddingVertical: 10, color: '#263f4d', backgroundColor: '#fff' }, controls: { flexDirection: 'row', flexWrap: 'wrap', gap: 30, marginTop: 16 }, controlLabel: { color: '#718394', fontSize: 11, fontWeight: 'bold', marginBottom: 6 }, arrowGrid: { flexDirection: 'row', flexWrap: 'wrap', width: 132, gap: 5 }, arrow: { width: 38, height: 34, borderWidth: 1, borderColor: '#aabfc7', borderRadius: 7, alignItems: 'center', justifyContent: 'center' }, arrowText: { color: '#123b52', fontSize: 19 }, sizeButtons: { flexDirection: 'row', gap: 7 }, sizeButton: { borderWidth: 1, borderColor: '#aabfc7', borderRadius: 8, paddingHorizontal: 10, paddingVertical: 9 }, sizeText: { color: '#0b777b', fontSize: 11, fontWeight: 'bold' }, dimensionRow: { flexDirection: 'row', gap: 10 }, dimensionField: { flex: 1, minWidth: 0 }, dimensionHint: { color: '#718394', fontSize: 11, lineHeight: 17, marginTop: 10 }, assignButton: { backgroundColor: '#123b52', borderRadius: 21, paddingHorizontal: 15, paddingVertical: 12, alignItems: 'center', marginTop: 18 }, assignButtonText: { color: '#fff', fontWeight: 'bold' }, empty: { backgroundColor: '#fff', borderWidth: 1, borderColor: '#ccdbe0', borderRadius: 17, padding: 28, alignItems: 'center', marginTop: 16 }, emptyTitle: { color: '#123b52', fontSize: 18, fontWeight: 'bold' }, emptyText: { color: '#718394', marginTop: 6 }, backdrop: { flex: 1, backgroundColor: 'rgba(8,34,48,.48)', justifyContent: 'center', padding: 18 }, modal: { width: '100%', maxWidth: 620, maxHeight: '90%', alignSelf: 'center', backgroundColor: '#fff', borderRadius: 19, padding: 20 }, modalHeader: { flexDirection: 'row', justifyContent: 'space-between', gap: 10 }, modalTitle: { color: '#123b52', fontSize: 21, fontWeight: 'bold' }, modalHint: { color: '#718394', fontSize: 12, marginTop: 3 }, close: { color: '#536b7a', fontSize: 27 }, inventoryList: { maxHeight: 430, marginTop: 12 }, inventoryRow: { flexDirection: 'row', alignItems: 'center', gap: 10, borderBottomWidth: 1, borderBottomColor: '#edf2f3', paddingVertical: 11 }, inventoryRowActive: { backgroundColor: '#e4f2ee' }, inventoryCheck: { width: 23, height: 23, borderWidth: 1, borderColor: '#aabfc7', borderRadius: 6, alignItems: 'center', justifyContent: 'center' }, inventoryCheckText: { color: '#0b777b', fontWeight: 'bold' }, inventoryCopy: { flex: 1, minWidth: 0 }, inventoryName: { color: '#123b52', fontWeight: 'bold' }, inventoryMeta: { color: '#718394', fontSize: 11, marginTop: 3 }, doneButton: { backgroundColor: '#123b52', borderRadius: 21, alignItems: 'center', padding: 12, marginTop: 14 }, doneText: { color: '#fff', fontWeight: 'bold' },
});
